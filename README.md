# marketplace-app-kit-example

Deploy repo for the **app-kit-example** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs app-kit-example --push`.

Coolify: base_directory `/`, dockerfile `/apps/app-kit-example/Dockerfile`.
